Project: GameGenie

Developer Instructions for Coding Assistants:

1. **Code Changes**: When modifying code, always run `npm test` to ensure agent configs remain valid.
2. **Agent Files**: Do NOT edit `agents/*/agent-config.json` without understanding Bedrock agent schema.
3. **Prompts**: System prompts are in `agents/*/prompts/system-prompt.md`—keep them age-appropriate and safe.
4. **Deployment**: Review `scripts/deploy.js` before suggesting production changes.
5. **Testing**: The test suite (`tests/agent.test.js`) validates agent configurations—it must pass before merging.

ApplyTo: GameGenie/**
