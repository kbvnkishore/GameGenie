{
  "name": "aws-bedrock-agent-version-control",
  "version": "1.0.0",
  "description": "Version control for AWS Bedrock agents using Infrastructure as Code",
  "main": "index.js",
  "scripts": {
    "deploy:dev": "node scripts/deploy.js dev",
    "deploy:staging": "node scripts/deploy.js staging",
    "deploy:prod": "node scripts/deploy.js prod",
    "test": "jest",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "keywords": [
    "aws",
    "bedrock",
    "agent",
    "version-control",
    "iac"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agent": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "dotenv": "^16.0.0",
    "yaml": "^2.0.0"
  }
}
