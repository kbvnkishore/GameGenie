# Copilot / Assistant Instructions

When editing this repository, follow these guidelines:

- Do not change `agent-config.json` semantics without CI tests updating.
- Keep `prompts/system-prompt.md` human-friendly and age-appropriate.
- Avoid introducing references to unrelated agents like "CustomerSupport".
- Run `npm test` after changes to validate agent config and scripts.
