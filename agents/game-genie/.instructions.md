Agent: GameGenie

Developer Instructions:
- Use `agent-config.json` to configure model and instructions.
- Prompts live under `prompts/system-prompt.md` and should be the single source of truth for system messages.
- Keep action definitions in `actions/action-group.json` in sync with any deployed lambda or API.

ApplyTo: agents/game-genie/**
